{
  "name": "web-to-pdf",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "esbuild --format=cjs --target=node20 --platform=node --bundle --outfile=dist.js --external:typescript index.js",
    "build": "node build.js",
    "build-windows": "node build-windows.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.10.0",
  "dependencies": {
    "puppeteer": "^24.7.2",
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.3"
  }
}
